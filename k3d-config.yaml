apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: taskflow
servers: 1
agents: 2
ports:
  - port: 8080:80
    nodeFilters: [loadbalancer]
  - port: 8443:443
    nodeFilters: [loadbalancer]
registries:
  create:
    name: taskflow-registry
    host: localhost
    hostPort: "5050"
