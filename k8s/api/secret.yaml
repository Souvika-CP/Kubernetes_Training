apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secret
  namespace: taskflow-dev
type: Opaque
data:
  # TRAINING ONLY — replace all values before any real deployment.
  # In production use Azure Key Vault + Secrets Store CSI Driver instead of this file.
  # Generate base64: echo -n "your-value" | base64
  MongoDb__ConnectionString: REPLACE_WITH_BASE64_ENCODED_CONNECTION_STRING
  Jwt__Key: REPLACE_WITH_BASE64_ENCODED_JWT_KEY
