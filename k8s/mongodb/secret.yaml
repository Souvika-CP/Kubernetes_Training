apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: taskflow-dev
type: Opaque
data:
  # TRAINING ONLY — replace with a strong password before any real deployment.
  # Generate your own: echo -n "YourStrongPasswordHere" | base64
  rootPassword: REPLACE_WITH_BASE64_ENCODED_PASSWORD
