apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  labels:
    {{- include "taskflow.labels" . | nindent 4 }}
type: Opaque
data:
  rootPassword: {{ .Values.mongodb.rootPassword | b64enc | quote }}
